import React from 'react';
import { View, Text, Image, ScrollView,StyleSheet, TouchableOpacity, StatusBar, SafeAreaView, navigation, TextInput,ImageBackground } from 'react-native';
import { DrawerNavigationItems} from 'react-navigation-drawer';


export default Sidebar = props =>{
    <ScrollView>
        <Text>Sidebar</Text>
    </ScrollView>
}
const style=StyleSheet.create({
    
});